# ADR-NNNN: 제목 (짧은 명사구)

- 날짜: YYYY-MM-DD
- 상태: 제안됨 / 승인됨 / 폐기됨 / 대체됨(ADR-XXXX로)

## 배경 (Context)

이 결정이 필요했던 상황. 어떤 제약, 어떤 문제, 어떤 대안들이 있었는지. "왜 지금 이걸 결정해야 했는가"가 드러나야 한다.

## 결정 (Decision)

실제로 무엇을 하기로 했는지 단정적으로 서술한다 ("~한다", "~를 사용한다"). 기각한 대안이 있다면 왜 기각했는지도 짧게 남긴다.

## 결과 (Consequences)

이 결정으로 인해 생기는 이득과 트레이드오프. 나중에 재검토해야 할 조건이 있다면 명시한다 ("~한 상황이 되면 재검토").

## 관련 문서

- 관련 `docs/architecture/*.md` 섹션, `PLAN.md` 항목, 커밋 등에 대한 링크
